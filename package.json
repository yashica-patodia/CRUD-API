{
  "name": "typescript-crud",
  "version": "1.0.0",
  "description": "1. Install typescript: npm i -g typescript\r 2. Generate the tsconfig.json file using tsc --init\r 3. Configure the tscconfig.json file and create a src directory to store all the .ts files\r 4. Command to compile the .ts file is tsc",
  "main": "index.js",
  "scripts": {
    "start:watch": "nodemon src/index.ts",
    "start": "npx ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.172.0",
    "@aws-sdk/util-dynamodb": "^3.172.0",
    "aws-sdk": "^2.1216.0",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  }
}